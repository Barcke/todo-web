# 本地开发环境配置示例
# 复制此文件为 .env.local 并根据实际情况修改配置

# 前端部署的子路径
# 本地开发通常使用根路径，可以不设置或设置为空字符串
# 如果需要测试子路径部署，可以设置为 "/todo-server"
NEXT_PUBLIC_BASE_PATH=

# 后端 API 完整地址
# 本地开发默认使用 http://localhost:6158/api
# 如果后端运行在其他地址，请修改此配置
BACKEND_API_BASE_URL=http://localhost:6158/api

