# 生产环境配置示例
# 复制此文件为 .env.production 并根据实际情况修改配置

# 前端部署的子路径
# - 根路径部署：不设置或设置为空字符串 ""
# - 子路径部署：设置为子路径，如 "/todo-server"
# 注意：必须以 "/" 开头，不能以 "/" 结尾
# 示例：NEXT_PUBLIC_BASE_PATH=/todo-server
NEXT_PUBLIC_BASE_PATH=

# 后端 API 完整地址
# 生产环境必须配置，不能使用默认值
# 注意：当前配置为直接使用后端地址（不使用代理），需要确保后端已配置 CORS 跨域支持
# 示例：BACKEND_API_BASE_URL=https://todo.barcke.com/api
BACKEND_API_BASE_URL=https://todo.barcke.com/api

